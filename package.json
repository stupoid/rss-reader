{
  "scripts": {
    "session-info": "node scripts/session-info.mjs",
    "dev": "bash build.sh && npx tsc --watch",
    "test": "npx playwright test --reporter=line",
    "package": "bash build.sh && cd dist && zip -r ../rss-reader.zip .",
    "lint-md": "npx markdownlint-cli2 '**/*.md' '#node_modules' '#dist'"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@playwright/test": "^1.61.1",
    "@types/chrome": "^0.2.2",
    "http-server": "^14.1.1",
    "markdownlint-cli2": "^0.22.1",
    "typescript": "^7.0.2"
  },
  "type": "module"
}
